const Message = (props) => {
  return (
    <div className="rb-message">
      <h2>This is a message box</h2>
      <p>
        In the event of an actual message, this box would contain more useful
        information. It could be something like a modal dialog, error pane, or
        other useful message that is determined by user action.
      </p>
    </div>
  );
};

export default Message;
