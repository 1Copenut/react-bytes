const Message = (props) => {
  return (
    <div className="rb-message">
      <h2>This is a message box</h2>
      <p>
        In the event of an actual message, this box would contain more useful
        information. It could be something like a news ticker, stock quotes, or
        other data that would appear based on asynchronous events.
      </p>
    </div>
  );
};

export default Message;
